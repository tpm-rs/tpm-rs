[package]
name = "tpm2-rs-base"
version = "0.0.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
open-enum = {version = "0.4.0"}
zerocopy = {version="0.7.0", features = ["derive"]}
marshal_derive = { path = "../marshal_derive" }